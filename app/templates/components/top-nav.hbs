{{yield}}
<nav class="navbar navbar-fixed-top" role="navigation">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">

                <h1 >Posts about things</h1>
            </div>
            <!-- Top Menu Items -->
            <ul class="nav navbar-right top-nav">

                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-user"></i> Account <b class="caret"></b></a>
                    <ul class="dropdown-menu">
                      {{#if isAuthenticated}}
                      <li class='dropdown-toggle'>{{#link-to "change-password"}}Change Password{{/link-to}}</li>
                      <li class='dropdown-toggle'><a href="#" {{action "signOut"}}>Sign Out</a></li>
                      {{else}}
                      <li class='dropdown-toggle'>{{#link-to "sign-up"}}Sign Up{{/link-to}}</li>
                      <li class='dropdown-toggle'>{{#link-to "sign-in"}}Sign In{{/link-to}}</li>
                      {{/if}}
                    </ul>
                </li>
            </ul>

            <!-- /.navbar-collapse -->
        </nav>
